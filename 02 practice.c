#include<stdio.h>

int main(){
    // int r = (8*5) / (5*4);

    // printf("the value of r is %d\n", r);

    // int x , y ;

    // printf("what is the value of x\n");
    // scanf("%d", &x);

    // printf("what is the value of y\n");
    // scanf("%d", &y);

    // printf("the value of (x*y) / (y*x) is %d\n", (x*y) / (y*x));

    // int v = 3^3 ;
    // printf("the value of v is %d\n", v);

    // char dt= '21 dec 2023';
    // printf("the value of dt is %d\n", dt);

    printf("the value of 3.0/8 - 2 is %f\n", 3.0/8 - 2 );

    int a; int b=a;
    int num;
    printf("Enter the number\n");
    scanf("%d", &num);
    printf("Divisibility test return: %d\n", num%97);
    


    return 0;
}